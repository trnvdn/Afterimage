# tests

- Unit / integration tests for the .NET services.
- `evals/` — RAG evaluation: precision/recall of semantic search against a
  labelled set, tracked over time so retrieval quality does not silently
  regress.
